import { title, pi, calcCircleArea } from './circleUtils.js'
console.log(pi)
const r = 4
console.log(`The area of a circle with radius ${r} is ${calcCircleArea(r)}`)

import calcUtils from './circleUtils.js'
console.log(calcUtils.calcCircleArea(5))